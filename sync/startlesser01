#!/bin/bash

#kill all running tasks is disabled!!!
#/var/www/sync/stopall

#play video 01 seamless looped without sync

/usr/bin/omxplayer --loop --no-osd -o both /media/internal/video/01* > /dev/null 2>&1 & echo $! &
